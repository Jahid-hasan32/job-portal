      // filterdJobs = filterdJobs.filter(({ jobLocation, maxPrice, salaryType, experienceLevel, employmentType, postingDate }) => (
      //   jobLocation.toLowerCase() === selectedString ||
      //   parseInt(maxPrice) <= parseInt(selected) ||
      //   salaryType.toLowerCase() === selectedString ||
      //   employmentType.toLowerCase() == selectedString
      // ));

    //   if (selected) {
    //     // location or salary type filtering
    //     filteredJobs = filteredJobs.filter((job) =>
    //       job.jobLocation.toLowerCase().includes(selected.toLowerCase()) ||
    //       job.salaryType.toLowerCase().includes(selected.toLowerCase()) ||
    //       job.maxPrice.toLowerCase().includes(selected.toLowerCase()) 
    //     );

